server:
  port: 9090

spring:
  config:
    import: optional:classpath:application-local.yml
  application:
    name: send-email-service
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    # Use a Gmail App Password via MAIL_PASSWORD; never commit real credentials.
    username: ${MAIL_USERNAME:bhurgrishahjahan28@gmail.com}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  mail:
    from: ${MAIL_FROM:${spring.mail.username}}
  payroll:
    company-name: "INTERNATIONAL CONSULTING ASSOCIATES PVT. LTD."
    company-address: "Office no.101, 1st Floor, Shafi Court, Merewether Road, Saddar, Karachi."
    company-email: "iconsulttechnologykhi@gmail.com"
    company-tel: "021 (35213058)"
